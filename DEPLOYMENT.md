# 🚀 MovieDex 自动化部署指南

## 概述
本项目已配置 GitHub Actions 实现自动化构建和发布 APK 到 GitHub Releases。

## 📋 自动化流程说明

### 触发条件
工作流会在以下情况下自动运行：
- 向 `main` 分支推送代码
- 创建针对 `main` 分支的 Pull Request
- 手动触发工作流（通过 GitHub 界面）

### 构建流程
1. 检出代码
2. 设置 JDK 17 环境
3. 安装指定版本的 Flutter (3.6.1)
4. 获取 Flutter 依赖
5. 构建 Release 版本 APK
6. 上传 APK 作为构建产物
7. 自动创建 GitHub Release 并上传 APK

## 🔧 配置要求

### 1. GitHub 仓库设置
确保你的仓库具有以下权限：
- `contents: write` 权限（已在工作流中配置）

### 2. 分支保护规则（可选但推荐）
建议为 `main` 分支设置：
- 要求 Pull Request 审核
- 要求状态检查通过
- 禁止强制推送

## 📦 发布版本管理

### 版本命名规则
- 自动版本：`v{运行号}.{尝试号}` (例如: v1.1, v2.1)
- 基于 GitHub Actions 运行编号自动生成

### Release 内容
每个 Release 包含：
- 自动生成的版本标签
- 详细的更新日志模板
- 编译时间戳
- 提交哈希值
- APK 文件附件

## 🛠️ 使用方法

### 方法一：自动触发
1. 将代码推送到 `main` 分支
2. GitHub Actions 会自动开始构建
3. 构建完成后自动发布到 Releases

### 方法二：手动触发
1. 访问 GitHub 仓库页面
2. 点击 "Actions" 标签
3. 选择 "Build and Release APK" 工作流
4. 点击 "Run workflow" 按钮

### 方法三：Pull Request 测试
1. 创建针对 `main` 分支的 Pull Request
2. 工作流会运行构建测试
3. 不会自动发布，仅用于验证构建

## 📊 查看构建状态

### 实时监控
- 访问仓库的 **Actions** 页面
- 查看当前和历史构建状态
- 点击具体构建查看详细日志

### 构建产物
- 成功构建的 APK 会显示在 **Artifacts** 中
- 可以下载测试版本进行本地测试

## 🔍 故障排除

### 常见问题

1. **构建失败**
   - 检查提交的代码是否有语法错误
   - 确认 Flutter 依赖是否正确
   - 查看详细的构建日志

2. **权限问题**
   - 确保仓库具有 `contents: write` 权限
   - 检查 GitHub Token 是否有效

3. **APK 未生成**
   - 确认 `flutter build apk --release` 命令执行成功
   - 检查构建路径是否正确

### 日志查看
在 GitHub Actions 页面可以查看完整的构建日志，包括：
- 环境设置过程
- 依赖安装详情
- 构建命令输出
- 错误信息和堆栈跟踪

## 🎯 最佳实践

### 开发流程建议
1. 在功能分支上开发新功能
2. 通过 Pull Request 合并到 `main` 分支
3. 让自动化流程处理版本发布

### 版本控制
- 保持语义化版本控制
- 在重大更新时考虑手动创建带描述的 Release
- 定期清理旧的构建产物

## 📱 用户下载指南

用户可以通过以下方式获取最新版本：

1. 访问仓库的 **Releases** 页面
2. 找到最新的 Release 版本
3. 下载对应的 APK 文件
4. 在 Android 设备上安装

## 🔄 持续集成优势

- ✅ 自动化构建，减少人为错误
- ✅ 每次提交都有可测试的版本
- ✅ 版本历史清晰可追溯
- ✅ 支持快速回滚到之前的版本
- ✅ 节省开发者的重复性工作

---
*此文档会随着项目发展持续更新*